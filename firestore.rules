rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // RÈGLES TEMPORAIRES POUR DEBUG ÉMULATEUR
    // ⚠️ ULTRA-PERMISSIVES - À RESTAURER APRÈS DEBUG ⚠️
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
